{
  "name": "link-mage",
  "version": "1.0.0",
  "description": "Summon ✨ magical links ✨",
  "private": true,
  "type": "module",
  "scripts": {
    "build:static": "rm -rf dist && mkdir -p dist && cp -r public/. dist/",
    "build:bg": "bun build src/background.ts --outdir dist --target=browser",
    "build:cs": "bun build src/copy-link.ts --outdir dist --target=browser --format=iife",
    "build:js": "bun run build:bg && bun run build:cs",
    "build": "bun run build:static && bun run build:js",
    "watch:bg": "bun build src/background.ts --outdir dist --target=browser --watch",
    "watch:cs": "bun build src/copy-link.ts --outdir dist --target=browser --format=iife --watch",
    "watch": "concurrently --names bg,cs --kill-others 'bun run watch:bg' 'bun run watch:cs'",
    "dev:firefox": "bun run build && concurrently --names watch,firefox --kill-others 'bun run watch' 'bunx web-ext run --source-dir dist'",
    "dev:chromium": "bun run build && concurrently --names watch,chromium --kill-others 'bun run watch' 'bunx web-ext run --source-dir dist --target chromium'",
    "dev": "bun run build && concurrently --names watch,firefox,chromium --kill-others 'bun run watch' 'bunx web-ext run --source-dir dist' 'bunx web-ext run --source-dir dist --target chromium'",
    "typecheck": "tsc --noEmit",
    "lint:ext": "bunx web-ext lint --source-dir dist",
    "package": "bun run build && bunx web-ext build --source-dir dist --artifacts-dir web-ext-artifacts --filename=link-mage-{version}.xpi --overwrite-dest",
    "package:source": "bash scripts/package-source.sh",
    "icons": "bash scripts/generate-icons.sh"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/chrome": "^0.0.270",
    "concurrently": "^10.0.0",
    "typescript": "^5.6.0",
    "web-ext": "^8.3.0"
  }
}
